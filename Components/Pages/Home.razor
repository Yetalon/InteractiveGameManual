@page "/"

<PageTitle>Maze Game</PageTitle>

<div class="row">
    <h1 class="d-flex justify-content-center">Welcome to Maze Game By: Alex Davis</h1>
    <div class="col-md-6 border border-black ">
        <h1 class="d-flex justify-content-center">How To win</h1>
        @if (!textWin){
            <div class="d-flex justify-content-center">
                <button @onclick=DisplayTextWin  class="btn btn-outline-dark btn-info btn-lg">Click Me!</button>
            </div>
        }
        else{
            <p style="margin-bottom: 20px;">To win the maze game you must adventure through the maze toward the exit marked
                E in a red box.
                You the player are marked P and in a green box. The items are marked W for weapon (light green) and O for
                Potions (gold). Lastly, there are monsters marked as a Red M. To beat the
                monsters i recommend you find a weapon to increase your power.</p>
            <div class="d-flex justify-content-center">
                <img src="https://github.com/Yetalon/Images/blob/main/InteractiveGameManual/Screenshot%20(2).png?raw=true" class="img-fluid h-25 w-auto" alt="Picture of Maze game">
            </div>
        }
    </div>
    <div class="col-md-6 border border-black">
        <h1 class="d-flex justify-content-center">Controls</h1>
        @if(!textControls){
            <div class="d-flex justify-content-center">
                <button @onclick=DisplayTextControls class="btn btn-outline-dark btn-info btn-lg">Click Me!</button>
            </div>
        }
        else{
            <p>For controls you can use either wasd or the arrow keys for moving in the maze. When in battle use either 1) to attack, 2) to block, or 3) to flee.</p>
            <div class="d-flex justify-content-center">
                <img src="https://github.com/Yetalon/Images/blob/main/InteractiveGameManual/Screenshot%202024-10-09%20165421.png?raw=true" class="img-fluid h-25 w-auto" alt="Picture of Maze game">
            </div>
        }
    </div>
</div>

@code {
    public bool textWin = false;
    public bool textControls = false;
    private void DisplayTextWin()
    {
        textWin = true;
    }
    private void DisplayTextControls()
    {
        textControls = true;
    }
}